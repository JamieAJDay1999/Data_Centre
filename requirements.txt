# Core data processing and numerical computing
pandas>=1.5.0
numpy>=1.23.0

# Optimization framework
pyomo>=6.5.0
# HiGHS solver backend used by the automatic Pyomo solver fallback.
highspy>=1.7.0
# Optional alternative solver backends:
# - GLPK (free, open-source): install system package or use conda
# - CBC (free, open-source): install system package or use conda
# - SCIP (academic license): download from https://www.scipopt.org/
# - CPLEX/Gurobi (commercial licenses available)

# Visualization
matplotlib>=3.6.0
seaborn>=0.12.0

# Interactive web dashboard (webapp/ + run_dashboard.py).
# Plotly is vendored client-side (static/dashboard/vendor), so only Flask is needed.
flask>=3.0.0

# Testing (optional, used in archive)
pytest>=7.0.0

